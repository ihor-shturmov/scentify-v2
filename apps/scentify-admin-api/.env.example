# ============================================================================
# Scentify Admin API - Environment Variables
# ============================================================================
# Copy this file to `.env` and fill in the values.
# NEVER commit the actual .env file to version control!
# ============================================================================

# ----------------------------------------------------------------------------
# Server Configuration
# ----------------------------------------------------------------------------

# Port for the admin API server (default: 3002)
PORT=3002

# Node environment: development, production, test
NODE_ENV=development

# ----------------------------------------------------------------------------
# Database
# ----------------------------------------------------------------------------

# MongoDB connection string
# For local development: mongodb://localhost:27017/scentify
# For production: Use your MongoDB Atlas connection string
MONGODB_URI=mongodb://localhost:27017/scentify

# ----------------------------------------------------------------------------
# Cloudinary (Image Upload Service)
# ----------------------------------------------------------------------------
# Sign up at https://cloudinary.com/ to get these credentials
# Required for image upload functionality

# Your Cloudinary cloud name (found in Dashboard)
CLOUDINARY_NAME=your_cloud_name

# Your Cloudinary API key (found in Dashboard > API Keys)
CLOUDINARY_API_KEY=your_api_key

# Your Cloudinary API secret (found in Dashboard > API Keys)
# KEEP THIS SECRET - never expose in frontend code!
CLOUDINARY_API_SECRET=your_api_secret
